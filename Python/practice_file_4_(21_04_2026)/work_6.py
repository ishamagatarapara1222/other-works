#6. Calculate area of circle (input as string → convert to float)

radius_text = input("Enter radius:")
radius = float(radius_text)
area = 3.14 * radius * radius

print("Area of Circle is ",area,"....")
