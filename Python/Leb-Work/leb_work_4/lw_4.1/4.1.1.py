#4.1.1
'''
Write a Python program that uses at least five built-in functions (e.g., len(), max(), sorted(), sum(), and type())
to perform operations on a list of numbers.
'''

numbers = [10, 5, 8, 20, 15]

print("Length:", len(numbers))
print("Maximum:", max(numbers))
print("Sorted:", sorted(numbers))
print("Sum:", sum(numbers))
print("Type:", type(numbers))
